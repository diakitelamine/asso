{% extends 'base.html.twig' %}


{% block body %}

    <div class="container my-5 small-container">
        <h1>Ma commande</h1>

        Adresse de livraison <br>
        {{ form_start(form) }}
            {{ form_row(form.adresses) }}
            <a href="{{path('app_account_adress_form')}}" class="small">Ajouter une adresse de livraison</a>
            <hr>
            {{ form_row(form.carriers) }}
        {{ form_end(form) }}

        
    </div>
  
{% endblock %}
